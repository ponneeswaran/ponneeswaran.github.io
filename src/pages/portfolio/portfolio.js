import { Container } from "react-bootstrap";

const Portfolio = () => {
    return(
        <Container className="Portfolio">
            <h1>Hi</h1>
        </Container>
    );
};

export default Portfolio;